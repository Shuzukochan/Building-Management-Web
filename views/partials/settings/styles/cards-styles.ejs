<style>
  /* Settings Cards */
  .settings-card {
    background: white;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    display: flex;
    flex-direction: column;
  }

  .settings-card-header {
    padding: 16px 20px;
    border-bottom: 1px solid #e5e7eb;
    background: white;
    border-radius: 8px 8px 0 0;
  }

  .settings-card-header h5 {
    margin: 0;
    color: #1f2937;
    font-size: 16px;
    font-weight: 600;
  }

  .settings-card-body {
    padding: 20px;
    display: flex;
    flex-direction: column;
  }

  .settings-description {
    color: #6b7280;
    font-size: 14px;
    margin-bottom: 20px;
  }

  /* Settings Sections */
  .settings-section {
    border: 1px solid #e5e7eb;
    border-radius: 6px;
    padding: 16px;
    margin-bottom: 16px;
    background: #f9fafb;
  }

  .settings-section:last-child {
    margin-bottom: 0;
  }

  .settings-section h6 {
    color: #374151;
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 12px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .settings-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 8px 0;
    border-bottom: 1px solid #e5e7eb;
  }

  .settings-info:last-child {
    border-bottom: none;
  }

  .settings-label {
    color: #6b7280;
    font-size: 14px;
  }

  .settings-value {
    color: #1f2937;
    font-weight: 500;
    font-size: 14px;
  }

  /* Buttons */
  .btn-settings {
    background: #3b82f6;
    border: 1px solid #3b82f6;
    color: white;
    border-radius: 6px;
    padding: 8px 16px;
    font-size: 14px;
    font-weight: 500;
    transition: all 0.2s ease;
  }

  .btn-settings:hover {
    background: #2563eb;
    border-color: #2563eb;
    color: white;
  }

  .btn-settings i {
    margin-right: 6px;
  }

  .btn-settings:disabled {
    background: #9ca3af;
    border-color: #9ca3af;
    color: #ffffff;
    cursor: not-allowed;
    opacity: 0.6;
  }

  .btn-settings:disabled:hover {
    background: #9ca3af;
    border-color: #9ca3af;
    color: #ffffff;
  }

  /* margin-top chỉ cho button bên trong card */
  .settings-card .btn-settings {
    margin-top: 12px;
  }

  /* Alerts */
  .settings-alert {
    border: none;
    border-radius: 6px;
    padding: 10px 12px;
    font-size: 13px;
  }

  .settings-alert-warning {
    background: #fef3c7;
    color: #92400e;
    border-left: 3px solid #f59e0b;
  }

  .settings-alert-info {
    background: #dbeafe;
    color: #1e40af;
    border-left: 3px solid #3b82f6;
  }

  /* Modal Styles */
  .modal-content {
    border: none;
    border-radius: 8px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.15);
  }

  .modal-header {
    background: white;
    border-bottom: 1px solid #e5e7eb;
    border-radius: 8px 8px 0 0;
    padding: 16px 20px;
  }

  .modal-title {
    color: #1f2937;
    font-size: 16px;
    font-weight: 600;
  }

  .modal-body {
    padding: 20px;
  }

  .form-group {
    margin-bottom: 16px;
  }

  .form-label {
    color: #374151;
    font-size: 14px;
    font-weight: 500;
    margin-bottom: 6px;
  }

  .form-control {
    border: 1px solid #d1d5db;
    border-radius: 6px;
    padding: 8px 12px;
    font-size: 14px;
    transition: border-color 0.2s ease;
  }

  .form-control:focus {
    border-color: #3b82f6;
    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.1);
  }

  .input-group-text {
    background: #f9fafb;
    border: 1px solid #d1d5db;
    border-left: none;
    color: #6b7280;
    font-size: 14px;
  }

  .form-text {
    color: #6b7280;
    font-size: 12px;
    margin-top: 4px;
  }

  /* Preview Results */
  .calibration-result {
    background: #f0f9ff;
    border: 1px solid #bae6fd;
    border-radius: 6px;
    padding: 12px;
    margin-top: 12px;
  }

  .calibration-result h6 {
    color: #0369a1;
    font-size: 14px;
    margin-bottom: 8px;
  }

  .result-item {
    display: flex;
    justify-content: space-between;
    margin-bottom: 4px;
    font-size: 13px;
  }

  .result-label {
    color: #0369a1;
  }

  .result-value {
    color: #0c4a6e;
    font-weight: 500;
  }

  /* Room Selector */
  .room-selector {
    min-width: 200px;
  }
  
  .room-selector .form-label {
    font-size: 13px;
    color: #6b7280;
    margin-bottom: 4px;
    font-weight: 500;
  }
  
  .room-selector .form-select {
    font-size: 14px;
    padding: 6px 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background: white;
    transition: border-color 0.2s ease;
  }
  
  .room-selector .form-select:focus {
    border-color: #3b82f6;
    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.1);
    outline: none;
  }

  /* Period card full height */
  .period-card {
    height: 100%;
  }

  .period-card .period-input {
    flex: 1;
  }
</style> 