<style>
  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f8f9fa;
    overflow-x: hidden;
  }
  
  .main-content {
    margin-left: 220px;
    padding: 15px;
    height: 100vh;
    overflow-y: auto;
    overflow-x: hidden;
    box-sizing: border-box;
  }
  
  .page-header {
    background: white;
    padding: 12px 20px;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    margin-bottom: 12px;
  }
  
  .page-header h1 {
    margin: 0;
    color: #1f2937;
    font-size: 20px;
  }
  
  .building-selector {
    min-width: 160px;
    font-size: 14px;
    padding: 8px 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background: white;
  }
  
  .building-selector:focus {
    border-color: #3b82f6;
    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.1);
  }
  
  /* Main content layout for viewport fit */
  .content-wrapper {
    display: flex;
    flex-direction: column;
    height: calc(100vh - 30px);
    gap: 12px;
  }
  
  .content-body {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 0;
    gap: 15px;
  }

  /* Settings Grid */
  .settings-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(450px, 1fr));
    gap: 20px;
    align-items: start;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .main-content {
      margin-left: 0;
      padding: 10px;
    }
    
    .settings-grid {
      grid-template-columns: 1fr;
      gap: 15px;
    }
  }
</style> 